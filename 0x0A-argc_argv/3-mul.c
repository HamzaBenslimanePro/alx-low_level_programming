#include "main.h"
#include <stdio.h>

/**
 * main - blabla
 * @argv: blabla
 * @argc: blabla
 * Return: Blabla
 */

int main(int argc, char *argv[])
{
	int M;

	M = argv[0] * argv[1];
	printf("%d\n", M);

	return (0);
}
